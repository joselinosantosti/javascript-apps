{{#each erros}}
	<div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}

<h3>Novo post</h3>

<div class="card mt-3">
	<div class="card-body">
		
	<form action="/admin/posts/novo" method="post">
	  <div class="form-group">
	    <label for="titulo">Titulo</label>
	    <input type="text" class="form-control" name="titulo" id="titulo" placeholder="Titulo" required="">

	    <label for="descricao">Descrição</label>
	    <input type="text" class="form-control" name="descricao" id="descricao" placeholder="Descrição" required="">

	    <label for="slug">Slug</label>
	    <input type="text" class="form-control" name="slug" id="slug" placeholder="Slug-separado-por-hifen" required="">

	    <label for="texto">Texto</label>
	    <textarea class="form-control" name="conteudo" placeholder="Texto" required=""></textarea>

	    <label for="categoria">Categoria</label>
	    <select name="categoria" class="form-control" required="">
	    	{{#each categorias}}
	    	<option value="{{_id}}">{{titulo}}</option>>
	    	{{else}}
	    	<option value="0">Nenhuma categoria</option>>
	    	{{/each}}
	    </select>
 
	  </div>

	  <div class="form-group">
	    <button type="submit" class="btn bg-purple">Salvar post</button>
	  </div>
	</form>
	</div>
</div>
