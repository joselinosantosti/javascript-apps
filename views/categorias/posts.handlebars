<h2>{{categoria.titulo}} </h2>

{{#each posts}}
	<div class="card mt-3">
		<div class="card-body">
			<h3>{{titulo}}</h3>
			<p>{{descricao}}</p>
			<hr>
			<div>
				<small>{{data}}</small>
			<br>
				<small>{{categoria.titulo}}</small>
			</div>
			 <a class="btn bg-purple" href="/post/{{slug}}" role="button">Leia mais</a>
		</div>
	</div>
{{else}}
	<h5>Nenhum post</h5>
{{/each}}